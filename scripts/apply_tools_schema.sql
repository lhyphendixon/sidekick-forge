-- Helper meta file that concatenates the Abilities (Tools) schema
-- Use Supabase SQL Editor or psql to run each segment as needed

-- PLATFORM DB: tools + agent_tools
-- Copy from migrations/add_tools_platform.sql

-- TENANT DB: tools only
-- Copy from migrations/add_tools_tenant.sql


